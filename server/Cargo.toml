[package]
name = "server"
version = "0.1.0"
edition.workspace = true

[[bin]]
name = "server"
path = "src/main.rs"

[dependencies]
engine = { workspace = true }
game = { workspace = true }
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread", "time"] }
